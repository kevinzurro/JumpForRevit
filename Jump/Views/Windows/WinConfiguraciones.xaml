<Window x:Class="Jump.Views.Windows.WinConfiguraciones"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Jump"
        xmlns:vm="clr-namespace:Jump.ViewModels"
        xmlns:userControl="clr-namespace:Jump.Views.UserControls"
        mc:Ignorable="d"
        MinHeight="600" MinWidth="800" WindowStartupLocation="CenterScreen"
        Height="600" Width="800">

    <Window.Resources>
        
        <ResourceDictionary>

            <ResourceDictionary.MergedDictionaries>

                <ResourceDictionary Source="/Jump;component/Styles/Estilos.xaml"/>
                <ResourceDictionary Source="/Jump;component/Languages/Language.xaml"/>

            </ResourceDictionary.MergedDictionaries>

            <DataTemplate DataType="{x:Type vm:ConfigGeneralViewModel}">

                <userControl:ConfigGeneral DataContext="{Binding}"/>

            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:ConfigEtiquetasViewModel}">
            
                <userControl:ConfigEtiquetas DataContext="{Binding}"/>
            
            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:ConfigCotasViewModel}">

                <userControl:ConfigCotas DataContext="{Binding}"/>

            </DataTemplate>

            <DataTemplate DataType="{x:Type vm:ConfigCotasProfundidadViewModel}">

                <userControl:ConfigCotasProfundidad DataContext="{Binding}"/>

            </DataTemplate>

        </ResourceDictionary>

    </Window.Resources>

    <Window.DataContext>
        
        <vm:ConfiguracionViewModel/>
        
    </Window.DataContext>
    
    <Grid Style="{StaticResource ConfigGrid}">

        <Grid.ColumnDefinitions>
            
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>       
            
        </Grid.ColumnDefinitions>
        
        <StackPanel Grid.Column="0" Background="White">

            <Label Style="{StaticResource LblTituloPrincipalInicial}"
                   Content="{StaticResource Conf10}">
            </Label>

            <Separator Style="{StaticResource SepDetAut}"/>
            
            <Button Style="{StaticResource BtnImagenYTexto}"
                    Command="{Binding CambiarVistaGeneralCommand}">

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Left">

                    <Image Style="{StaticResource ImgImagenYTexto}"
                           Source="/Jump;component/Resources/Boton_Config_General.png">
                    </Image>

                    <TextBlock Style="{StaticResource TxbImagenYTexto}"
                               Text="{StaticResource Conf1-1}">
                    </TextBlock>

                </StackPanel>

            </Button>

            <Button Style="{StaticResource BtnImagenYTexto}"
                    Command="{Binding CambiarVistaEtiquetasCommand}">

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Left">

                    <Image Style="{StaticResource ImgImagenYTexto}"
                           Source="/Jump;component/Resources/Boton_Config_Etiquetas.png">
                    </Image>

                    <TextBlock Style="{StaticResource TxbImagenYTexto}"
                               Text="{StaticResource Conf2-1}">
                    </TextBlock>

                </StackPanel>

            </Button>

            <Button Style="{StaticResource BtnImagenYTexto}"
                    Command="{Binding CambiarVistaCotasCommand}">

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Left">

                    <Image Style="{StaticResource ImgImagenYTexto}"
                           Source="/Jump;component/Resources/Boton_Config_Cotas.png">
                    </Image>

                    <TextBlock Style="{StaticResource TxbImagenYTexto}"
                               Text="{StaticResource Conf3-1}">
                    </TextBlock>

                </StackPanel>

            </Button>

            <Button Style="{StaticResource BtnImagenYTexto}"
                    Command="{Binding CambiarVistaCotasProfundidadCommand}">

                <StackPanel Orientation="Horizontal" 
                            HorizontalAlignment="Left">

                    <Image Style="{StaticResource ImgImagenYTexto}"
                           Source="/Jump;component/Resources/Boton_Config_CotaProfundidad.png">
                    </Image>

                    <TextBlock Style="{StaticResource TxbImagenYTexto}"
                               Text="{StaticResource Conf4-1}">
                    </TextBlock>

                </StackPanel>

            </Button>

        </StackPanel>

        <ContentControl Grid.Column="1"
                        Margin="10,0,0,0"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        Content="{Binding VistaActual}">
        </ContentControl>
        
    </Grid>
    
</Window>
